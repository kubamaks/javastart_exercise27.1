spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Mynewpassword1!
    url: jdbc:mysql://localhost:3306/users?useSSL=false&serverTimezone=Europe/Warsaw
  liquibase:
    contexts: prod
  sql:
    init:
      mode: never